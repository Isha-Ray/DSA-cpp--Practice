#include<iostream>
using namespace std;

int main()
{
 /* int a=10;
    int b=a--;
    cout<<b<<" "<<a;
*/

    // Compartion operator
    // 1 or 0

    /*
    int a,b;
    cout<<"Enter the value of a: ";
    cin>>a;
    cout<<"Enter the value of b: ";
    cin>>b;

    // ==

    // if(a==b)
    // cout<<"yes";
    // else
    // cout<<"no";

    // > , < , >= , <=, !=

    if(a!=b)
    cout<<"yes";
    else
    cout<<"no";
    */


    /* Logical Operator ------

    int a,b,c;
    cin>>a>>b>>c;

    if(a>b&&a>c)
    cout<<"Yes";
    else
    cout<<"No";

    */

    /* OR

    char name = 'i';

    if(name=='a'||name=='e'||name=='i'||name=='o'||name=='u')
    cout<<"Vowel";
    else
    cout<<"Consonant";

    */


    /* NOT

    cout<<!28<<endl; // 0
    cout<<!0<<endl; // 1
    cout<<!1<<endl; // 0
    */


/*Bitwise Operator 
    
Operator	        Symbol	                            Description	                                    Example (in binary)
AND	                   &	                    Sets bit to 1 if both bits are 1	                     1101 & 1011 = 1001
OR	                   |	                    Sets bit to 1 if at least one is 1	                     1101 | 1011 = 1111
XOR	                   ^	                    Sets bit to 1 if bits are different	                     1101 ^ 1011 = 0110
NOT	                   ~	                        Inverts each bit (1 → 0, 0 → 1)	                    ~1101 = 0010 (on 4 bits)
LEFT SHIFT	           <<	                    Shifts bits left, fills with 0s	                         0010 << 1 = 0100
RIGHT SHIFT	           >>	                Shifts bits right, fills with 0s or sign bit	             1000 >> 2 = 0010

*/

    



    

}